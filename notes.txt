#  notes of installation
https://bbc.github.io/bbcat-orchestration-docs/installation/#installing-ffmpeg-and-ffprobe-manually


ffmpeg ffprobe ffserver

commands to install coping binaries
1.-
sudo mkdir -p /usr/local/bin/
sudo cp ./ffmpeg /usr/local/bin
sudo chmod ugo+x /usr/local/bin/ffmpeg
2.-
open -e ~/.bash_profile
3.-
export PATH="/usr/local/bin:$PATH"